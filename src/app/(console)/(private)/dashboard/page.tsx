import AdminDashboard from './_admin';
// import MentorDashboard from './mentor';
// import StudentDashboard from './student


const Dashboard = () => {
  // @todo: depending on the users role, we will render the appropriate dashboard
  return <AdminDashboard />;
};

export default Dashboard;